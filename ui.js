function pageUpdate() {
        //this code keeps the page updated with the latest variables, and runs basically constantly.
        
}

function init() {
    console.log("Initial scripts started")
    getThrottle
    //this runs on page load, put any scripts that must run on startup here
    
    
}